<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="24dp"
    tools:context=".MainActivity">

    <!-- Header Text -->
    <TextView
        android:id="@+id/headerTextMain"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="48dp"
        android:gravity="center"
        android:text="@string/main_header_text"
        android:textSize="18sp"
        android:textColor="@color/black" />

    <!-- Choose Color Button -->
    <Button
        android:id="@+id/mainButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:text="@string/main_button_text"
        android:textSize="16sp"
        android:textStyle="bold"
        android:padding="16dp" />

    <!-- Selected Color Button (shows when color is selected) -->
    <Button
        android:id="@+id/selectedColorButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="ORANGE is your favorite color"
        android:textSize="16sp"
        android:textStyle="bold"
        android:padding="16dp"
        android:visibility="gone" />

</LinearLayout>